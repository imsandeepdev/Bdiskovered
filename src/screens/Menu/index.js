import * as React from 'react';
import {useEffect, useState} from 'react';
import {
  View,
  Image,
  Text,
  TouchableOpacity,
  ScrollView,
  Alert,
} from 'react-native';
import { StoryScreen } from '../../components';
import R from '../../res/R';

const Menu = props => {
  

  return (
    <StoryScreen >
        <View
        style={{flex:1, borderWidth:1, backgroundColor:R.colors.whatsAppColor}}
        >

        </View>
     
      
    </StoryScreen>
  );
};



export default Menu;
